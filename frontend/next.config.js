/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add other Next.js configurations here if needed
  // For example, environment variables, redirects, etc.
};

module.exports = nextConfig;
